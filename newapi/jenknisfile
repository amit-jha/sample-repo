#! /usr/bin/groovy
pipeline{
    agent{

    }
    stages{
        stage("Test"){
            steps{
                echo("Testing")
            }
        }
        stage("Deploy"){
            steps(){
                echo("Deploying")
            }
        }
    }
}

// docker run --rm --name jenkins -p 3030:3030 -p 50000:50000 -d -v /Users/amijha0/docker/volumes/jenkins:/var/jenkins_home -v /var/run/docker.sock:/var/run/docker.sock jenkinsci/blueocean